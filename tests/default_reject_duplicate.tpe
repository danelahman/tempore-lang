(* Correctly rejected by the typechecker, under the default 'time-lower-bound'
   grading monoid: an operation may be given at most one default
   implementation. *)

operation Log : string ~> unit # 1

default Log msg = delay 1
default Log msg = delay 2
