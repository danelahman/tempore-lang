(* Correctly rejected by the typechecker, under the 'traces-upper-bound'
   grading monoid: the runtime bounds of an operation must be declared under the
   timed-trace grading monoids. *)

operation Heat : unit ~> unit # {Heat}
