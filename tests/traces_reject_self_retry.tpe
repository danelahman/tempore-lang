(* Correctly rejected by the typechecker, under the 'traces-upper-bound'
   grading monoid: a compound operation may not name itself, since its runtime
   bounds are computed from those of the operations its grade names. A retry
   has to be expressed through a smaller operation instead. *)

operation Send : string ~> unit # {Send | Send; Send}
